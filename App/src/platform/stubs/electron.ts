/**
 * Electron stub — used when building for mobile/web.
 * Prevents import errors for any code that still imports 'electron' directly.
 */
export const ipcRenderer: null = null;
export const ipcMain: null = null;
export const app: null = null;
export const BrowserWindow: null = null;
export default null;
